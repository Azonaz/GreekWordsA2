import SwiftUI

struct VerbView: View {
    var body: some View {
        ZStack {
            Color.grayDN
                .edgesIgnoringSafeArea(.all)
        }
    }
}

#Preview {
    VerbView()
}
